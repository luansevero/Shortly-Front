import styled from "styled-components";

export default function Header(){
    return(
        <Container>

        </Container>
    )
}

const Container = styled.header`
    display:flex;
    align-items:center;
`